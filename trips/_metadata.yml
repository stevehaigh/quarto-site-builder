# Defaults applied to every trip page in this directory
freeze: true

filters:
  - trips

# "Published" is the wrong word for the start of a trip.
language:
  title-block-published: "Trip starts"

format:
  html:
    toc: true
    toc-depth: 2
